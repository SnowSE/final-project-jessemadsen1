@page
@model FinalProject.Pages.DeleteChannelModel
@{
}

<div class="col-md-12 col-sm-12 mx-auto mb-5">
    <form method="post">
        <input type="hidden" asp-for="Channel.ID" />
        <div class="col-md-8">
            <h3>Are you sure you want to delete this Channel?</h3>
            <br>
            <h2>
                @Model.Channel.Title
            </h2>
            <p><em>@Html.DisplayFor(modelItem => Model.Channel.Body)</em></p>
            <br>
            <hr />
        </div>
        <div class="row">
            <div class="col-md-6 col-sm-12  mb-5">
                <button type="submit" class="btn btn-outline-danger">Delete</button>
                <a asp-page="./Index">
                    <input type="button" class="btn btn-outline-secondary" value="Cancel" />
                </a>
            </div>
        </div>
    </form>
</div>

@section scripts{
    <partial name="_ValidationScriptsPartial" />
}